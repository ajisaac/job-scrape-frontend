import * as React from "react";

class Batch extends React.Component{
  constructor(props) {
    super(props);
  }

  render() {
    return (
        <div>Batch Page</div>
    );
  }
}

export default Batch;